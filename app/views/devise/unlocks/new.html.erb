<div class="c-login">
  <div class="c-login__title">
    Resend unlock instructions
  </div>

  <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "c-login__form" }) do |f| %>
    <%= f.error_notification %>
    <%= f.full_error :unlock_token %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true, input_html: { class: 'c-login__input' } %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Resend unlock instructions", class: 'c-login__button' %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
