<div class="c-devise">
  <div class="c-devise__title">
    Resend unlock instructions
  </div>

  <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "c-devise__form" }) do |f| %>
    <%= f.error_notification %>
    <%= f.full_error :unlock_token %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true, input_html: { class: 'c-devise__input' } %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Resend unlock instructions", class: 'c-devise__button' %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
