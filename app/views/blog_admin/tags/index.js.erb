var newContent = "<h1>Sorry, Didn't Find Anything</h1>"

if (<%= @tags.length %> > 0) {
  newContent = "<%= j render 'blog_admin/shared/tag_list' %>"
}

document.querySelector('.js-admin-tags__list').innerHTML = newContent
