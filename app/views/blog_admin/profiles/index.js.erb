var newContent = "<h1>Sorry, Didn't Find Anything</h1>"

if (<%= @profiles.length %> > 0) {
  newContent = "<%= j render 'blog_admin/shared/profile_list' %>"
}

document.querySelector('.js-admin-profiles__list').innerHTML = newContent
